import { SharedResourcesDeleteResponseDTO } from "../../dto/resources-delete-response.dto";

describe("SharedResourcesDeleteResponseDTO", () => {
  it("should be defined", () => {
    expect(new SharedResourcesDeleteResponseDTO()).toBeDefined();
  });
});
