import { LineStringsResponseDTO } from "../line-string-response.dto";

describe("LineStringsResponseDTO", () => {
  it("should be defined", () => {
    expect(new LineStringsResponseDTO()).toBeDefined();
  });
});
